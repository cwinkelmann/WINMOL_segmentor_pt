"""Learned-vectorization mini-project (dev-only, never shipped in the wheel).

This file exists so pytest names the test package `learned_vectorization.tests` instead of
`tests`, which would collide with the repo's top-level `tests` package (both carry an
`__init__.py`, and both contain a `test_model.py`) and break plain `pytest` at collection.
"""
